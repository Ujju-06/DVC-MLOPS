Create a git repo and clone it in local.
Create mycode.py amd add code to it. (it will save a csv file to a new "data" folder)
Do a git add-commit-push before initilizing dvc.

pip install dvc

now we do "dvc init" (creates .dvcignore, .dvc)

create an folder "mkdir s3"

Now we do "dvc remote add -d myremote s3"

next "dvc add data/" ------------------------------------> This will through error  ==== fir ye 2 commands recommend krega use chlana than is same command ko run krna { Because initially we were tracking data/ folder from git so now we remove it for DVC to handle }

Again we do "dvc add data/" (creates data.dvc) than "git add .gitignore data.dvc"

Now "dvc commit" and than "dvc push"

Do a git add-commit-push to mark this stage as first version of data saved.

Now make changes to mycode.py to append a new row in data, check changes via "dvc status"

Again "dvc commit" and than "dvc push"  so that changes data.dvc me id change ho jaye or push krne pr new data me kiye hue changes s3 me push ho jayega

Than git add - commit - push ( we are saving v2 of our data at this point)

Check dvc/git status, everything should be upto data. 

Now repeat steps "dvc commit" , "dvc push" Than git add - commit - push ( we are saving v3 of our data at this point)

